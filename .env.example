# Local development only
# For Railway deployment, set these in Railway dashboard

# Secret key for Flask sessions (generate with: python -c "import secrets; print(secrets.token_hex(32))")
SECRET_KEY=your-secret-key-here

# Database URL (optional - uses SQLite by default locally)
# For PostgreSQL: postgresql://user:password@localhost:5432/pharmacy
# DATABASE_URL=

# Flask environment
FLASK_ENV=development
FLASK_DEBUG=0
